export default {
    setProperty(state, property) {
        state.property = property
    },
    setPropertyComments(state, comments) {
        state.propertyComments = comments
    },
    setProperties(state, properties) {
        state.properties = properties
    },
}
